# 記帳系統v1－Web 驗證測試紀錄

- 測試日期：2026-08-10
- 系統名稱：SelfBank v1
- 使用範圍：個人記帳
- 測試環境：Windows、Node.js、vinext Web Runtime

## 驗證摘要

| 編號 | 驗證項目 | 方法 | 結果 |
|---|---|---|---|
| V1-01 | 首頁 HTTP 回應 | 本機 Web Server 請求 | 通過（HTTP 200） |
| V1-02 | 正式產品建置 | `npm run build` | 通過 |
| V1-03 | 程式規範及無障礙靜態檢查 | `npm run lint` | 通過 |
| V1-04 | 產品標題及核心區塊輸出 | Server-rendered HTML 自動測試 | 通過 |
| V1-05 | 新增交易邏輯 | 原始碼自動測試＋型別建置 | 通過 |
| V1-06 | 瀏覽器本機保存 | localStorage 實作檢查 | 通過 |
| V1-07 | CSV 解析及匯入入口 | FileReader 實作檢查 | 通過 |
| V1-08 | 載具格式限制 | 表單 pattern 驗證檢查 | 通過 |
| V1-09 | 響應式桌面／手機樣式 | CSS 斷點靜態檢查 | 通過 |
| V1-10 | 瀏覽器點擊流程自動化 | MCP 實際操作 CSV 匯入與去重 | 通過 |
| V1-11 | 銀行與載具消費去重 | 金額、店家相似度、日期容差自動測試 | 通過 |
| V1-12 | 每月固定扣款管理 | 本機保存、每月合計與下次扣款日 | 通過 |
| V1-13 | 外部帳號連動移除 | Google／Apple／iCloud 入口與相關介面檢查 | 通過 |
| V1-14 | Python 資料層 | SQLite 交易、固定扣款與索引 | 通過 |
| V1-15 | Python CSV 去重 | 載具比對與重複匯入測試 | 通過 |
| V1-16 | FastAPI HTTP 介面 | 健康檢查、交易、固定扣款與 CSV API | 通過 |
| V1-17 | 加密銀行 PDF | 正確密碼可解密、錯誤密碼拒絕、密碼必填 | 通過 |
| V1-18 | PDF 防重複匯入 | PDF 交易沿用載具／銀行交易去重規則 | 通過 |
| V1-19 | 台北富邦 PDF 版型 | 帳務日期、摘要、支出、存入與附註欄位解析 | 通過 |
| V1-20 | SQL Server 資料層 | Windows Authentication、交易與固定扣款 CRUD | 通過 |
| V1-21 | 前後端資料連動 | 交易、CSV、PDF、固定扣款讀寫本機 API | 通過 |
| V1-22 | 完整交易紀錄 | 全部／支出／收入與分類交叉篩選、統計摘要 | 通過 |
| V1-23 | 每月固定收支 | 固定收入／固定支出、分類顯示與淨收入預估 | 通過 |
| V1-24 | 三頁資訊架構 | 交易首頁、固定收支頁、財務分析頁 | 通過 |
| V1-25 | 財務異常分析 | 支出最高分類、可變占比、月底預估與歷史資料狀態 | 通過 |

## 版本調整紀錄

### 2026-08-10－固定扣款與外部帳號調整

- 新增每月固定扣款管理，可設定名稱、金額、扣款日與分類。
- 新增每月固定支出合計、下次扣款日及暫停項目功能。
- 固定扣款保存在目前瀏覽器，銀行 CSV 匯入時仍套用防重複規則。
- 曾規劃 Google 與 Apple／iCloud 官方帳號連結入口。
- 依使用者決定，已移除 Google、Apple 與 iCloud 的連動入口、設定說明及相關介面。
- 外部帳號的密碼、權杖及個人訂閱資料均未蒐集或保存。

### 2026-08-10－Python 後端優化

- 保留 React／TypeScript 前端與現有視覺化操作。
- 新增 FastAPI Python 後端，前後端採分離架構。
- 新增 SQLite 資料庫，保存交易及每月固定扣款。
- 新增交易日期與去重查詢索引，並啟用 WAL 模式。
- 將銀行 CSV 與載具發票的去重邏輯實作於 Python 資料層。
- 新增交易、固定扣款、CSV 匯入及健康檢查 API。
- Python 後端尚未部署前，線上網站維持瀏覽器本機保存，不影響原本功能。

### 2026-08-10－密碼保護銀行 PDF 匯入

- 資料匯入視窗新增「加密 PDF」模式，PDF 檔案與開啟密碼皆為必填。
- 密碼只在該次 HTTP 請求的記憶體中使用，不寫入設定檔、資料庫、localStorage 或操作紀錄。
- 後端以 Python 解密文字型 PDF，解析交易後沿用金額、店家名稱與日期容差的防重複規則。
- 錯誤密碼回傳明確錯誤且不匯入任何資料；測試使用假密碼，不含使用者真實憑證。
- 目前通用解析格式為 `YYYY/MM/DD 名稱 金額`；掃描影像 PDF 或銀行特殊欄位需另加 OCR／版型解析。
- 線上站尚未部署 Python API，因此 PDF 按鈕會保持停用並顯示設定提示；CSV 功能不受影響。

### 2026-08-10－台北富邦交易明細版型

- 依使用者提供的台北富邦「臺幣活期存款交易明細」畫面新增表格解析。
- 讀取帳務日期、摘要、支出金額、存入金額與附註，不保存帳號、即時餘額或交易時間。
- 支出金額建立 expense，存入金額建立 income。
- 刷卡消費與刷卡退貨優先以附註店家名稱比對載具發票，提高重複消費辨識率。
- 測試資料使用虛構欄位內容，未收錄畫面中的真實帳號。

### 2026-08-10－本機 SQL Server 儲存

- 已驗證 `localhost:1433` 可透過 Windows Authentication 連線。
- 不直接使用 `master` 保存資料；已建立 `SelfBank` 專用資料庫。
- 新增 SQL Server 交易、固定扣款資料表、日期索引及防重複查詢索引。
- 前端設定本機 API 後，交易、CSV、PDF 與固定扣款皆改由 SQL Server 讀寫；無 API 時仍保留 localStorage 備援。
- 連線採 Trusted Connection 與 Trust Server Certificate，不保存 Windows 密碼。
- SQL Server 寫入、讀取、更新與測試資料清除整合測試均通過。
- 雲端網站無法直接存取個人電腦的 localhost；此設定供本機 SelfBank 使用。

### 2026-08-10－交易紀錄重新設計

- 「最近交易」升級為「所有支出與收入」，完整顯示符合條件的交易，不再只顯示前 8 筆。
- 可切換全部收支、只看支出或只看收入，並與分類下拉選單交叉篩選。
- 即時計算篩選結果筆數、收入、支出與收支差額。
- 日期改為完整年月日，並保留分類、資料來源及正負金額辨識。
- 新增無結果提示、ARIA 篩選標籤與手機版單欄統計配置。
- 前端自動測試新增交易紀錄篩選驗證，並完成瀏覽器互動測試。
- 瀏覽器驗證：全部 6 筆、只看支出 5 筆、交通分類 1 筆；收入項目在支出模式正確隱藏，重設後恢復 6 筆。
- 瀏覽器統計驗證：收入 62,000 元、支出 4,620 元、差額 57,380 元，且無前端錯誤或警告。

### 2026-08-10－每月固定收支

- 「固定扣款」升級為「固定收支」，新增固定收入與固定支出兩種類型。
- 新增表單可選固定收入或固定支出；固定收入自動歸入收入分類。
- 總覽分別顯示每月固定收入、固定支出及預估淨收入。
- 可切換全部、固定支出或固定收入清單，金額以正負號與顏色辨識。
- SQLite 與 SQL Server 新增向後相容的 `type` 欄位，既有固定扣款預設為固定支出。
- API、SQLite、SQL Server 與前端本機資料均支援 `expense`／`income`。

### 2026-08-10－三頁架構與財務分析

- 移除總覽功能，根路徑 `/` 直接顯示交易紀錄。
- 固定收入／固定支出移至獨立 `/recurring` 頁面。
- 新增 `/analysis` 財務分析頁，包含最近 7 天、支出分類、本月盈虧與儲蓄率。
- 分析統計將固定支出計畫與目前可變支出拆開，顯示占比與預估總支出。
- 分析頁最上方新增「本月財務異常」，顯示最高支出分類、可變支出占比、月底支出預估及歷史資料累積狀態。
- 需要近 6 個月資料的平均、消費次數與連續上升判斷，在資料不足時明確標示「資料累積中」，避免產生虛構警示。
- 多路由建置驗證通過：`/`、`/recurring`、`/analysis` 均可獨立輸出，且內容不交叉混入。

## 人工驗收步驟

1. 點擊「新增交易」，輸入名稱、金額、日期和分類，確認交易出現在清單且統計數字更新。
2. 點擊「手機載具」，輸入以 `/` 開頭的 8 碼條碼，重新整理後確認仍然存在。
3. 準備 CSV：`date,title,amount,type,category`，匯入後確認筆數與內容。
4. 將視窗縮窄至手機寬度，確認底部導覽列、卡片及表單無水平溢出。
5. 關閉再開啟頁面，確認個人交易資料保留於相同瀏覽器。
6. 啟動 Python API 並設定前端 API URL，以測試用加密文字 PDF 驗證正確密碼可匯入、錯誤密碼被拒絕，且重複交易會略過。

## 已知限制與安全說明

- v1 僅顯示及保存使用者輸入的手機載具，尚未連接財政部正式 API。
- 銀行資料可採 CSV 或密碼保護 PDF 匯入；系統不蒐集網銀帳號、簡訊驗證碼，也不保存 PDF 密碼。
- localStorage 是單一裝置的便利保存方式，不適合跨裝置同步或多人使用。
- 正式銀行／發票同步需另行完成官方申請、OAuth 授權、金鑰管理、傳輸加密及個資安全評估。
- 台北富邦解析以 PDF 內含可選取文字與表格線為前提；掃描影像版本仍需 OCR。

## 結論

SelfBank v1 已達到個人使用 MVP 的建置、靜態驗證與正式網站互動驗證標準。

### 2026-08-10：正式網站切頁錯誤修正與驗證

- 問題：原本的快速預載導覽在正式執行環境產生 RSC prefetch 錯誤，點擊側邊選單後可能只改變選取狀態而沒有載入對應頁面。
- 修正：三個主頁面選單改用穩定的完整頁面導覽，避免不相容的預載流程。
- 建置驗證：`/`、`/recurring`、`/analysis` 均成功建置；自動測試 6 項全部通過。
- 正式網站互動驗證：依序點擊「交易紀錄 → 固定收支 → 財務分析 → 交易紀錄」，網址、標題及頁面內容均正確切換。
- 固定收支驗證：共顯示 4 筆固定收支；點擊「固定收入」後按鈕狀態正確，清單只保留 1 筆固定收入。
- 財務分析驗證：「本月財務異常」、「最近 7 天」、「本月盈虧」、「儲蓄率」、「固定與可變支出」及「預估月底總支出」皆正常顯示。
- 頁面隔離驗證：交易紀錄頁不混入分析內容；固定收支與分析頁不混入交易紀錄標題。
- 瀏覽器錯誤驗證：完整操作流程結束後，瀏覽器主控台錯誤與警告皆為 0。

### 2026-08-10：台北富邦 PDF 匯入與交易明細表

- 暫停手機載具功能：移除側邊選單、載具設定視窗及分析頁載具卡片。
- 交易紀錄改為銀行明細表格，欄位依台北富邦 PDF 排列：帳務日期、交易時間、摘要、支出金額、存入金額、即時餘額、附註。
- PDF 解析器會保留上述七個原始欄位，並依支出／存入欄位判斷交易類型與金額。
- 台北富邦刷卡交易以附註中的店家名稱作為交易名稱，同時保存原始摘要與附註。
- SQLite 與 SQL Server 交易資料表新增 PDF 明細欄位；既有資料庫啟動時會自動補上欄位，舊資料仍可正常顯示。
- 加密 PDF 密碼仍只用於單次解密，不寫入設定檔、瀏覽器、資料庫或紀錄文件。
- 重複匯入仍會經過防重複規則，已存在的交易會略過。
- 驗證結果：Web 測試 6/6 通過；Python 測試 7 項通過，SQL Server 實機整合測試在未啟用時正確略過。

### 2026-08-10：純 PDF 匯入與交易紀錄 PDF 匯出

- 匯入介面只保留 PDF 檔案與 PDF 開啟密碼，不顯示銀行名稱，也不再提供 CSV 選項。
- 移除前端 CSV 讀檔程式、CSV 匯入 API 與資料層 CSV 解析入口。
- 交易紀錄頁新增帶下載圖示的「匯出 PDF」按鈕。
- 匯出檔名格式為 `SelfBank-交易紀錄-YYYY-MM-DD.pdf`。
- 匯出內容採 A4 橫向版面，包含標題、匯出時間、總筆數，以及帳務日期、交易時間、摘要、支出金額、存入金額、即時餘額、附註七欄表格。
- PDF 內嵌繁體中文字型，避免在其他 PDF 閱讀器出現亂碼或缺字。
- PDF 匯入與匯出皆由本機 Python API 處理，資料來源為本機 SQL Server；PDF 密碼不保存。
- 驗證結果：Web 測試 6/6 通過；Python 測試 8 項通過；匯出 PDF 已實際產生並轉成圖片檢查，中文字、欄位、金額、格線與版面均正常。

### 2026-08-10：交易五欄表與資料庫狀態修正

- 交易紀錄表調整為五欄：類型、名稱、金額、日期、分類。
- 類型資料表固定包含收入、支出；分類資料表固定包含餐飲、日用品、娛樂、交通、股票、醫療。
- SQLite 與 SQL Server 均新增 `transaction_types` 與 `transaction_categories` 參照資料表及查詢 API。
- 既有分類會自動轉換：日常採買／帳單／其他轉為日用品，學習轉為娛樂，收入分類轉為股票。
- 移除交易頁頁首的通知圓圈與新增交易按鈕；保留交易卡片內的新增功能。
- 移除右下角「無法連接本機資料庫服務」浮動訊息。
- 左側資料庫狀態改為依 API 實際回應判斷，不再只因設定 API 網址就誤顯示已連接。
- 資料庫實測：SQL Server 程序正在執行，但本機 API 8000 埠未啟動，且目前測試連線出現 SQL Server 加密／Windows 驗證錯誤，因此截圖當下並未真正完成網站資料庫連線。
- 驗證結果：Web 測試 6/6 通過；Python 測試 9 項通過，SQL Server 實機整合測試維持選配。

### 2026-08-10：本機 API 與 HomeAccounting 真實連線驗證

- Python FastAPI 已啟動於 `127.0.0.1:8000`，呼叫 `/health` 可收到 API 回應。
- `/health` 現在會透過 pyodbc 執行實際 SQL 查詢；只有 SQL Server 查詢成功、後端為 `sqlserver` 且資料庫為 `HomeAccounting` 時才回報已連線。
- 前端已移除 seed、localStorage 與 fallback 假資料。API 或資料庫失敗時，交易與固定收支不顯示替代資料。
- 左下角狀態已改為即時 health check：可分別顯示「已連接本機 SQL Server」、「SQL Server HomeAccounting 未連線」與「本機 Python API 未連線」。
- 本次實測 SQL Server 回傳錯誤 4060 / 18456：Windows 使用者 `LAPTOP-MUS11254\\alexl` 無法開啟 `HomeAccounting`。因此 `/health` 正確回傳 HTTP 503，前端不得顯示已連線。
- 驗證結果：Web 建置與測試 6/6 通過；Python 測試 9 項通過，SQL Server 寫入整合測試因資料庫未授權而未執行。

### 2026-08-10：建立 HomeAccounting 與正式資料表

- 已由 Windows 使用者 `LAPTOP-MUS11254\\alexl` 建立 `HomeAccounting`；該使用者目前具有 SQL Server sysadmin 權限。
- 已建立 `transactions`、`transaction_categories`、`transaction_types`、`recurring_payments` 四張資料表及必要索引與欄位限制。
- 類型參照資料為收入、支出；分類參照資料為餐飲、日用品、娛樂、交通、股票、醫療。
- 建立完成時，交易與固定收支筆數皆為 0，未寫入假資料。
- API 已重新啟動並監聽 `127.0.0.1:8000`；`/health` 實測回傳 HTTP 200、`status=connected`、`backend=sqlserver`、`database=HomeAccounting`。
- SQL Server 實機 CRUD 測試通過，測試產生的交易與固定收支已在清理階段刪除；再次檢查兩張資料表皆為 0 筆。

### 2026-08-10：資料表與欄位中文註解

- 四張資料表及其全部欄位已加入 SQL Server `MS_Description` 中文擴充屬性。
- 共建立 31 筆中文註解：4 筆資料表說明與 27 筆欄位說明。
- 註解包含交易日期、名稱、分類、金額、收入／支出類型、PDF 原始欄位、固定扣款日及啟用狀態等用途。
- 完整對照表與 SSMS 查詢方式已記錄於《資料庫欄位說明》。

### 2026-08-10：交易備註與既有資料編輯

- 交易紀錄表新增「備註」顯示欄位；沒有備註時顯示 `—`。
- 新增交易時可輸入最多 200 字備註。
- 每筆交易提供「編輯」操作，可修改類型、名稱、金額、日期、分類與備註。
- 後端新增 `PATCH /transactions/{item_id}`，更新成功會回傳完整交易；不存在的編號回傳 404。
- 保留交易原始來源與 PDF 原始欄位，編輯一般欄位時不覆寫來源資料。
- 系統未提供刪除按鈕、`DELETE` 前端請求或交易刪除 API，既有交易不可刪除。
- 驗證結果：Web 建置與測試 6/6 通過；Python 一般測試 10 項通過；HomeAccounting 實機建立、編輯備註及清理測試通過。
- 實機測試紀錄已清除，交易與固定收支資料表皆維持 0 筆。

### 2026-08-10：財務分析圖表真實資料修正

- 修正支出圓餅圖原先使用固定色塊比例、與分類金額不一致的問題。
- 圓餅圖現在依本月實際支出分類與金額動態計算每一段比例，圖例、金額、百分比與色塊使用同一份資料。
- 修正「最近 7 天」原先使用固定長條高度 `[42,72,35,88,55,28,64]` 的錯誤。
- 最近 7 天現在逐日比對交易日期，只加總實際支出；長條高度、每日金額及日均值皆由同一組資料計算。
- 本月統計只計入當月交易，不再把其他月份交易混入本月盈虧與支出分析。
- 本月或最近 7 天沒有支出時顯示明確空狀態，不繪製示意圖或假圖表。
- 驗證包含程式測試、真實 API 數值比對及本機瀏覽器畫面檢查。
